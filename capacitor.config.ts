import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'synergicportal.claim',
  appName: 'Claim UX',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
